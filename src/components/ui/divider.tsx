/**
 * A simple divider component to separate sections of content.
 * @returns {JSX.Element} The divider component.
 */

const Divider = () => {
  return (
    <div className="w-20 h-0.5 dark:bg-cyber-green-400 mx-auto my-4"></div>
  );
};

export default Divider;
